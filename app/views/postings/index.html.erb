<h1>投稿一覧</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @postings.each do |posting| %>
  <tr>
    <td><%= posting.content %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しく投稿する', new_posting_path %>
